import React from 'react';
import HeroSlider from './HeroSlider';

const Hero: React.FC = () => {
  return (
    <section className="relative" >
      <HeroSlider />
    </section>
  );
};

export default Hero;