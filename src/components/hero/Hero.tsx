import React from 'react';
import HeroSlider from './HeroSlider';

const Hero: React.FC = () => {
  return (
    <section className="pt-20 md:pt-24">
      <HeroSlider />
    </section>
  );
};

export default Hero;